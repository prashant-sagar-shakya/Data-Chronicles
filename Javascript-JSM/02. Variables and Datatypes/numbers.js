// Numbers in javascript
let wholeNumber = 10;
let decimalNumber = 10.5;
// No need to mention the data type because javascript is a dynamic language

console.log(wholeNumber, decimalNumber);

const string = 'Hello World';
const num = 10;
const result = string / num;
console.log(string / num); // It gonna gives NaN as a result which means not a number
console.log(typeof result);