// SetTimeout is a function that calls another function after a specified delay (in milliseconds).
// The general syntax of setTimeout is:
// setTimeout(function, milliseconds, param1, param2, ...)

// Example:
function sayHi() {
    console.log('Hi!');
}

setTimeout(sayHi, 2000); // Outputs 'Hi!' after 2 seconds
